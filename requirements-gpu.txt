# Install this file on Windows with an NVIDIA driver and CUDA-capable GPU.
# The PyTorch CUDA wheels are selected explicitly instead of the CPU-only build.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.14.0+cu126
torchvision==0.29.0+cu126

-r requirements.txt